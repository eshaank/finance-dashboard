# TODO: Screener & Watchlist domain — stock screening filters, saved watchlists
# Note: Watchlists will require database tables (Supabase Postgres)
from fastapi import APIRouter

router = APIRouter(prefix="/screener")
