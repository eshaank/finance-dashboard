- The goal of this one is to allow the user to create customizeable widgets so they can kind of use it similar to bloomberg terminal

- For early iteration, we can just make widgets a set size (so chart would be bigger than say a financial widget)
    - These can then just be moved around, etc.
    - eventually i would like to allow them to be resized
- Mobile version will just have specific size widgets to keep it simple. 
    - or have a different UI all together for mobile. 
    - (I like) Or this is just not available on the mobile site and you need desktop for the dash board

- Maybe a terminal style feature to allow users to either user their keyboard, or just use the UI to open a widget. 

